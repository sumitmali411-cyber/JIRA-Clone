# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new, empty repository for a JIRA Clone project. No code has been committed yet. Update this file as the project is set up with build tools, frameworks, and architecture decisions.

## Getting Started

Once the project is initialized, document the following here:
- Package manager and install command (e.g., `npm install`, `pnpm install`, `bun install`)
- Dev server command (e.g., `npm run dev`)
- Build command (e.g., `npm run build`)
- Test command and how to run a single test (e.g., `npm test -- --testPathPattern=<name>`)
- Lint command (e.g., `npm run lint`)
